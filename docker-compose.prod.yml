services:
  app:
    image: ghcr.io/veerbal1/homestead:${TAG}
    pull_policy: always
    ports: 
      - "80:8080"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - API_KEY=${API_KEY}
    restart: unless-stopped
